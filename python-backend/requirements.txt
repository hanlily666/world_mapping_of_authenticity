# Python Backend Requirements for Voice Matching Service
# Install with: pip install -r requirements.txt

# Core dependencies
flask==3.0.0
flask-cors==4.0.0
numpy==1.26.2
scikit-learn==1.3.2
requests==2.32.3  # Required by SpeechBrain

# SpeechBrain and dependencies
speechbrain==1.0.0
torch==2.1.1
torchaudio==2.1.1
huggingface_hub==0.23.5  # Compatible with SpeechBrain 1.0.0 (versions <0.24.0)

# Audio processing backend (CRITICAL for torchaudio to work!)
soundfile==0.12.1  # Provides audio file I/O backend for torchaudio

# Additional utilities
python-dotenv==1.0.0
gunicorn==21.2.0  # For production deployment

